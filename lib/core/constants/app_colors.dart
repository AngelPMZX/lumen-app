import 'package:flutter/material.dart';

class AppColors {
  // Primarios
  static const primary = Color(0xFF6C63FF);
  static const primaryLight = Color(0xFF9D97FF);
  static const primaryDark = Color(0xFF4A42DB);

  // Acentos
  static const secondary = Color(0xFF10B981);
  static const accent = Color(0xFFFF6B6B);
  static const streak = Color(0xFFFF9500);

  // Fondos
  static const background = Color(0xFFF8F9FE);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceVariant = Color(0xFFF0EEFF);

  // Texto
  static const textPrimary = Color(0xFF2D2D3A);
  static const textSecondary = Color(0xFF6B7280);
  static const textOnPrimary = Color(0xFFFFFFFF);

  // Estados de ánimo
  static const moodHappy = Color(0xFF10B981);
  static const moodCalm = Color(0xFF3B82F6);
  static const moodNeutral = Color(0xFFFBBF24);
  static const moodSad = Color(0xFF8B5CF6);
  static const moodAnxious = Color(0xFFEF4444);
}